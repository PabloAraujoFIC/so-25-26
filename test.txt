# ---- Información básica ----
authors
authors -l
authors -n
date
hour
date -d
date -t
infosys
help
help authors dir historic

# ---- PID y directorio ----
getpid
getpid -p
pid
pid -p
cwd
getcwd
pwd
chdir .
cd ..
cwd

# ---- Pantalla ----
cls

# ---- Historial ----
historic -count
historic -clear

# ---- Creación y borrado de ficheros/dirs ----
create tmpdir
dir -d tmpdir
chdir tmpdir
create -f file.txt
dir
setdirparams short nolink nohid norec
getdirparams
dir file.txt
chdir ..
erase tmpdir/file.txt
dir -d tmpdir
delrec tmpdir

# ---- Gestión de ficheros abiertos ----
create -f testfile.txt
open testfile.txt ro
listopen
dup 3
listopen
close testfile.txt
listopen

# ---- Memoria ----
mem -funcs
mem -vars
mem -blocks
mem -all
mem -pmap
malloc 64
# Sustituye <PTR> por la dirección que muestre el malloc anterior:
# memfill <PTR> 16 0x41
# memdump <PTR> 16
# free <PTR>

# ---- mmap / shared ----
shared
shared -create 12345 256
shared
shared -free 12345
mmap sample.txt rw
mmap
mmap -free sample.txt

# ---- Procesos ----
fork
jobs
deljobs -term
# Ejecuta una orden externa en segundo plano:
/bin/sleep 1 &
jobs
deljobs -term

# ---- Señales/UID ----
uid -get

# ---- Exec de programa externo (foreground) ----
exec /bin/echo Exec_works

# ---- Salida ----
exit
